# MeetingAssistant 需求文档

> 版本：v0.1（2026-07-23）
> 平台：iOS 26+（SwiftUI，现有工程 deployment target 26.2）

## 1. 产品概述

一款面向会议场景的 iOS 智能助手。App 实时将会议语音转成文字，并**对"问题"高度敏感**：一旦检测到有人提问，立即调用 LLM 作答——LLM 能直接回答的就直接流式输出答案；回答不了的（涉及私有领域信息），则检索本地知识库（RAG）后再作答。所有问题会被**去除口水话、整理成书面表达**并记录存档。

核心体验指标：**快、流式**。从"问题说出口"到"答案开始出现"的延迟越短越好。

## 2. 核心功能需求

### F1 实时语音转文字
- 基于 iOS 26 的 `SpeechAnalyzer` + `SpeechTranscriber` **本地识别**：零网络延迟、免费、离线可用、音频不出设备。
- 利用 volatile results（未定稿结果）做实时上屏，finalized results（定稿结果）用于问题检测和存档。
- **识别语言可选**（设置页「识别语言」：自动/中文/English）：本地模型是**单语种**的，实测用中文模型识别纯英文会议会严重出错（Kubernetes → "copernaties horn"，问号丢失导致问题完全漏检），故开放为用户可选；所选语言不可用时回退其它语言而非中断转写。云端识别由服务端自动判别语言，不受此设置影响。
- **中英混合**：选中文时对夹杂英文术语有一定容错；纯英文会议应选 English 或改用云端识别。
- 架构上抽象 `TranscriptionProvider` 协议，本地识别为默认实现，**预留云端 ASR**（讯飞/阿里/Whisper 等）接入点，后续可在设置中切换。
- 录音期间保持屏幕常亮或启用 background audio mode，避免转写中断。

### F2 问题检测（高敏感度）
- 目标：**高召回**——宁可多触发，不可漏掉问题。
- 三层检测，兼顾速度与覆盖（真机实测后由两级升级为三层）：
  1. **本地快速触发**（毫秒级）：对每段 finalized 文本做规则判断——疑问词（什么/为什么/怎么/多少/是不是/能不能/吗/呢/how/what/why/when/which/can/should…）、选择疑问句（还是）、提问引导语（想问/请问/问一下/有个问题）、疑问句式、问号等；
  2. **LLM 确认**：触发后交给 LLM，由同一次调用完成"是否真问题"判定 + 口水话清洗 + 回答（见 F3），非问题则丢弃；
  3. **LLM 漏检兜底扫描**：未扫描转写累计 ≥100 字或 ≥20 秒时，把该片段（带 60 字重叠）+ 已捕获问题列表交给 LLM 找漏网问题，结束会议前做收尾扫描；找到的问题走标准 QA 管线。
- 辅助入口：**手动提问按钮**——把最近转写尾段直接交给 QA 管线提取并回答，作为最后保险。
- **volatile 提前检测**（可开关，默认开）：连续说话时 ASR 可能几十秒不定稿（真机实测 46 秒连续语音合并为约 25 秒的单段），在未定稿文本上即时跑规则检测，命中后待文本再增长 20 字（说话人已越过问题）或稳定 2 秒即触发出卡；该段定稿时跳过重复检测。
- 兜底扫描与提前检测均可在设置页开关，兜底扫描另有会议内快速开关。
- 检测窗口需处理**跨句问题**（问题被断句切开）：拼接最近 N 秒的上下文一起送检。
- 同一问题去重：短时间内重复/相似问题不重复触发。

### F3 智能问答（LLM 直答 → 知识库兜底）
- **第一步·直答**：检测到问题后立即调用 LLM，流式输出。Prompt 约定结构化流式格式，一次调用同时返回三件事：
  ```
  <q>清洗后的问题</q>          ← 先输出，立即上屏并记录
  <a>流式答案……</a>            ← LLM 能答：直接流式输出
  或
  <kb/>                        ← LLM 判断答不了（私有信息/领域知识）：输出标记
  ```
- **第二步·知识库兜底**：收到 `<kb/>` 后，将问题向量化 → 在本地知识库检索 Top-K 相关片段 → 携带片段**与会议近期上下文**重新调用 LLM → 流式输出答案，并标注引用来源。
- **指代消解**：清洗问题时，若「这个项目/刚才的方案」等指代能从会议上下文确定，替换为具体名称（使检索 query 精确化）；知识库回答时同样结合上下文判断所指，多个候选无法确定时分别列出各候选数据。
- **语言一致性**：清洗后的问题、直答、知识库回答、会议纪要均跟随会议/提问本身的语言（英文会议全程英文，中文知识库资料同样可给出英文答案）。因 prompt 与资料多为中文会「带偏」模型，采用按 CJK 字符占比的确定性判定后注入显式语言指令。
- **延迟优化**：知识库检索与第一步调用**并行**预执行（检索是本地毫秒级操作），兜底时无需等待。
- 回答携带会议近期上下文（最近若干轮转写），使指代（"这个方案""刚才说的"）可被理解。
- LLM 不可用/超时/网络失败时：问题仍照常清洗记录，答案区显示失败状态并可手动重试。

### F4 问题清洗与记录
- 由 F3 第一次 LLM 调用顺带完成：去除"嗯、啊、就是说、那个、然后、um、like"等口水话及重复停顿，整理为简洁书面问句。
- 每条记录保存：清洗后问题、原始转写片段、答案、答案来源（LLM 直答 / 知识库+引用文档）、时间戳、所属会议。
- 会议内以**问题卡片流**呈现，会后可回顾全部问答。

### F5 本地知识库（文档导入 + 向量检索）
- 支持导入 PDF / TXT / Markdown（iOS 文件选择器 / 分享扩展）。
- 导入流程：解析文本 → 按段落/长度切块（chunk）→ 本地生成向量（`NLContextualEmbedding`，支持中英、免费离线）→ 存入本地索引。
- 检索：问题向量化后余弦相似度 Top-K（个人知识库规模下暴力检索足够，无需向量数据库）。
- 管理界面：文档列表、导入进度、删除文档（连带清理其索引块）。

### F6.3 提词器（发言稿）
- **发言稿库**：粘贴新建或从文件导入（PDF/TXT/Markdown，复用知识库解析），可编辑、删除；首页「发言稿」入口。
- **会议中调用**：实时会议页顶部按钮选稿 → 浮层**只盖住转写区**，问答卡片仍完整可见可操作，转写与问答管线照常运行（宽屏下只覆盖左栏）。
- **随语音自动跟随**：用实时转写在稿件中定位当前念到的行，自动滚到视野中央并加粗高亮，念过的内容淡出。匹配用字符二元组覆盖率（复用 `KnowledgeRetriever.lexicalScore`），容忍口误、识别误差与即兴插话；窗口限定在当前位置附近（向前 10 行、向后 2 行），匹配度不足时保持原位不乱跳。
- 可关闭跟随改为手动滚动；字号可调（16–44pt）。

### F6.5 iPad 适配
- **首页**：`NavigationSplitView`——iPad 左列表右详情同时可见，iPhone 自动折叠为堆栈导航（行为不变）。
- **实时会议页**：按 `horizontalSizeClass` 切换——regular（iPad、iPhone Max 横屏）左右分栏，转写 45% / 问答 55%（显式声明比例，避免被内容固有尺寸左右）；compact 保持上下分区。
- **可读宽度**：纪要、转写全文、问答记录等长文本限制在 760pt 内居中，避免大屏上行长过长影响阅读。
- 侧栏行只显示时长与问题数（标题已含日期时间），窄侧栏下不换行挤压。

### F6 会议记录管理
- 会议会话（Session）为基本单位：开始/结束录制，自动保存完整转写文本 + 问答记录。
- 历史会议列表（时间、时长、问题数）、会议详情（转写全文 + 问题卡片）。
- 持久化用 SwiftData。

### F7 设置：可配置多家 LLM
- 抽象 `LLMProvider` 协议，实现 **OpenAI 兼容格式**（chat/completions + SSE 流式），即可兼容 OpenAI / DeepSeek / 通义 / Claude（经兼容端点）等绝大多数服务。
- 用户可添加多个 Provider 配置：名称、base URL、API key、模型名；选择当前使用的 Provider。
- **API key 存 Keychain**，不落盘明文。
- 提供"连接测试"按钮验证配置有效。

## 3. 非功能需求

| 维度 | 要求 |
|---|---|
| 转写延迟 | 本地识别，实时上屏（volatile results，秒级以内） |
| 问答首字延迟 | 问题定稿 → 答案开始流式输出，目标 < 2~3s（含 LLM 首 token） |
| 流式输出 | 转写、清洗后问题、答案全部流式渐进呈现 |
| 隐私 | 音频不出设备；仅问答相关文本发送给 LLM；API key 存 Keychain |
| 离线能力 | 转写、知识库检索离线可用；仅 LLM 问答需要网络 |
| 稳定性 | 长时间录音（1h+）不中断、不爆内存；网络抖动不影响转写 |

## 4. 技术选型

| 模块 | 选型 |
|---|---|
| UI | SwiftUI，iPhone / iPad 通用（size class 自适应，非两套代码） |
| 语音识别 | iOS 26 `SpeechAnalyzer` + `SpeechTranscriber`（本地），`AVAudioEngine` 采集；`TranscriptionProvider` 协议预留云端 |
| 问题检测 | 本地规则触发 + LLM 确认 |
| LLM 接入 | OpenAI 兼容 API + SSE 流式，`URLSession` 实现；`LLMProvider` 协议多家可配 |
| 向量化 | `NLContextualEmbedding`（本地、中英支持） |
| 向量检索 | 内存余弦相似度 Top-K，向量持久化随 SwiftData |
| 持久化 | SwiftData（会议、转写、问答、知识库文档/块） |
| 密钥 | Keychain |

## 5. 关键流程（问答 pipeline）

```
麦克风 → AVAudioEngine → SpeechTranscriber
    ├─ volatile 文本 → 实时字幕上屏
    └─ finalized 文本 → 存档
            └─ 本地规则检测疑问 ──命中──▶ 并行：
                 ├─ ① LLM 调用（近期上下文 + 候选问题）
                 │      └─ 流式返回 <q>清洗问题</q> → 上屏+记录
                 │            ├─ <a>答案流…</a> → 流式上屏 → 记录（来源：直答）
                 │            └─ <kb/> → 取②结果 → 二次 LLM（带检索片段）
                 │                          └─ 答案流式上屏 → 记录（来源：知识库+引用）
                 └─ ② 本地向量检索 Top-K（预取，毫秒级）
```

## 6. 版本规划

- **P0（MVP）**：F1 本地转写 + F2 问题检测 + F3 直答（含失败降级）+ F4 清洗记录 + F6 会议保存 + F7 单 Provider 配置 ✅ 已完成并真机验证
- **P1**：F5 知识库导入与 RAG 兜底 ✅ 已完成（向量不可用时自动降级文本匹配检索）；F7 多 Provider 管理 ✅ 已完成（SwiftData 记录 + 生效快照，预设模板 DeepSeek/通义/OpenAI，key 按 Provider 独立存 Keychain）
- **P2**：云端 ASR 切换（Fish Audio）✅ 已完成——设置页「语音识别」可在本地识别与 Fish Audio 云端间切换；云端按停顿分段（3~8 秒）上传识别，术语与标点准确率显著更高，问号直接利于问题检测；批式 API 无 volatile 流，上传期间显示识别中占位。会议纪要总结 ✅（详情页「纪要」Tab，LLM 基于转写+问答流式生成结构化纪要并存档，可重新生成）、导出/分享 ✅（详情页分享按钮导出 Markdown 文件：纪要+问答记录+转写全文）、说话人分离（待做）

## 7. 风险与待确认

| 项 | 说明 |
|---|---|
| 中英混合识别 | 中文 locale 对英文术语的识别效果需真机实测；不佳时考虑云端 ASR 提前 |
| 问题检测误报 | 高敏感度必然带来误报（反问、引述），靠 LLM 确认过滤；卡片支持手动删除 |
| `<q>/<a>/<kb/>` 流式解析 | 需做增量解析器，容忍 LLM 输出格式偶发偏差 |
| 长会议 token 成本 | 问答携带的上下文窗口需限制长度（如最近 2~3 分钟转写） |
| 录音权限/后台 | 需麦克风与语音识别权限文案；后台持续录音需 audio background mode |
